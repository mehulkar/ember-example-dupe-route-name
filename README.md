# ember-example-dupe-route-name

When a route is defined twice, calling `visit()` in a test helper or appInstance.visit()
with a URL works fine, but calling transitionTo() with a URL fails. 

See test cases.
